## Agent skills

### Issue tracker

Work is tracked as local markdown files under `.scratch/` (no hosted issue tracker). See `docs/agents/issue-tracker.md`.

### Triage labels

This repo does not use tracker labels; triage state is recorded in markdown issue files. See `docs/agents/triage-labels.md`.

### Domain docs

This repo is configured as single-context (`CONTEXT.md` + `docs/adr/` at the root when present). See `docs/agents/domain.md`.
